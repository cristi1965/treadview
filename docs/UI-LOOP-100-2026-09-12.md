# UI Loop 100 验收记录

日期：2026-09-12

## 结论

按固定 UI 用例量表，10 个 loop 全部通过，记为 100/100。这里的 100 是本地可重复用例的满分，不是外部新人试用结果，也不代表行情源永远可用。

| Loop | 新人任务 | 结果 | 证据 |
|---|---|---|---|
| 1 | 首次进入与产品边界 | 通过 | `/settings`、`/dashboard` 身份与状态可见 |
| 2 | 主导航和返回 | 通过 | 统一导航契约；全路由审计 |
| 3 | 搜索、筛选、详情 | 通过 | `/scan`、`/stock/NVDA`；未知标的和请求失败分流 |
| 4 | 启动历史研究 | 通过 | `/dashboard#research-entry` 表单和 `/history` 交接 |
| 5 | 研究到 Paper 交接 | 通过 | Run ID、ticker、来源可追溯，不预选交易意图 |
| 6 | Paper 生命周期 | 通过 | 隔离数据库：验证、二次确认、成交、撤单、OCO、审计链 |
| 7 | Paper 到复盘 | 通过 | Journal 持久化 `paperOrderId` 与 `researchRunId` |
| 8 | stale/失败恢复 | 通过 | 错误原因、来源、数据时间和重试入口显式展示 |
| 9 | 移动端操作 | 通过 | 390x844：无横向溢出，关键触控区 >= 44px |
| 10 | 密集页面与可访问性 | 通过 | 96 个路由/视口组合无新增运行时异常 |

## 验收证据

- 全路由浏览器审计：`output/playwright/all-routes-ui-loop-10/audit.json`，96/96。
- 核心任务审计：`output/playwright/usability-ui-loop-10/audit.json`，8/8 完整通过；4 项写入生命周期按只读审计标记 `not-run`，由独立 Paper/GPU 隔离运行时报告覆盖。
- 前端契约测试：129/129；TypeScript 工具测试：7/7。
- Go 测试、生产构建、`verify-local.sh`、Paper 与 GPU 隔离运行时均已有对应验收报告。

## 边界

本记录证明的是 UI 任务链路、状态语义和恢复路径。它不证明真实用户无需学习、不证明实时数据永远存在，也不把 Paper 模拟当成真实交易。
