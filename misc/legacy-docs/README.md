# docs/archive

历史交接/总结类 Markdown 放在这里，仅供追溯，**不是**日常开发入口。

## 已移出 / 删除

- `recovered_source/`（曾约 18GB，含 `node_modules` 与探针 dumps）已从本仓移除，不再跟踪。
- 若仍需对照材料，见仓库根旁的外部备份，或重新用 site-recovery 工具拉取；**不要**再把 `node_modules` 提交进 `docs/`。

## 日常请读

- `/AGENTS.md`
- `/docs/README.md`
- `/docs/ARCHITECTURE.md` · `DATA.md` · `TESTING.md` · `AI_NATIVE.md`
